name: pull-request
"on":
  pull_request:
    branches:
    - master
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: install
        run: testing/setup.sh
